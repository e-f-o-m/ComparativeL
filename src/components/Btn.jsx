import React from 'react';
import { json } from 'react-router-dom';

const Btn = () => {
  let name = "Btn Test";
  return (
      <div className="btn">
        <button onClick={() => {}}>{name}</button>
    </div>
  );
};

export default Btn