import React from 'react';

const Btn = () => {
  let name = "Btn Test";
  return (
      <div className="btn">
        <button onClick={() => {}}>{name}</button>
    </div>
  );
};

export default Btn